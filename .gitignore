# Sierra pipeline outputs --> keep gold, ignore the rest
results/*
!results/gold/
!results/gold/*

# Python caches
__pycache__/
.pytest_cache/

# Input sequence data (may remove this upon confirmation data is public)
data/*

# Evaluation run artifacts and task specs (local-only for now)
eval/runs/
eval/tasks/